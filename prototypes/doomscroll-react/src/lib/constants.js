/** Abbreviated day names Mon–Sun, matching the weeklyData array indices. */
export const DAY_NAMES = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];

/** Feet of vertical scroll distance per post viewed (approximate). */
export const FEET_PER_POST = 9;
